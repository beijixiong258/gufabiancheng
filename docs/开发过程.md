# 开发过程

更新时间：2026-08-19

## 1. 当前进度

- 用户注册、登录、Token刷新、退出和个人资料管理已经完成。
- 管理员用户管理已经完成，管理员权限由同一个认证拦截器检查；修改接口已改用 `GuanliyuanXiugaiDTO`，空白密码保留原值并执行参数校验。
- 记录基本增删改查已经完成；会话已实现新增、查看、列表和删除，消息已实现发送和列表；消息表已改为使用MySQL自增 `BIGINT` 主键，相关数据归属检查已经完成。
- AI对话Service和HTTP入口已经实现三个命令，并统一由 `ChatService.duihua()` 根据 `command` 处理。
- 已新增 `DuihuaDTO`，统一接收 `huihuaId`、`command`、`neirong` 和编辑器当前 `zhengwen`。
- `DuihuaController` 和统一的 `ChatService.duihua()` 已经改用 `DuihuaDTO`，编辑器当前正文已经传入AI上下文，并限制为最多2000个字符。
- `DuihuaController` 已统一使用 `/api/duihua`，接口为 `POST /api/duihua/chat`、`GET /api/duihua/getlist` 和 `GET /api/duihua/test`。
- 原 `XiaoxiXinjianDTO` 已删除。
- AI对话后端新流程已经完成：使用 `DuihuaZhuangtai`，不再统计固定回合数，普通对话按用户意图判断状态，`OVER` 只保存普通AI消息而不更新记录正文。
- 已使用真实接口完成MiMo连接和结构化JSON联调，返回结果通过后端严格字段校验。
- AI对话业务已按传统Service结构拆分为 `linggu.service.ChatService` 接口和 `linggu.service.impl.ChatServiceImpl` 实现；模型响应、解析、记忆和提示词相关类保留在 `linggu.ai`。`ChatServiceImpl` 只通过 `ChatMemory.get(huihuaId)` 读取近期消息和历史摘要，可见消息写入和失败补偿统一由 `XiaoxiService` 完成。当前不接入 `MessageChatMemoryAdvisor`，业务流程不调用 `ChatMemory.add()` 和 `clear()`。
- 模型JSON解析和协议校验已提取到 `AIResponseParser`。
- 普通用户业务和管理员用户管理统一由 `YonghuService` 处理，两个Controller分别保留普通接口和管理员接口。
- 用户Service已提取 `zhanghaoCunzai()` 和 `zhanghaoZhanyong()`，统一复用新增与修改场景的账号查询规则。
- 消息类型已改用 `XiaoxiLeixing` 枚举，并通过 `@EnumValue` 显式映射为数据库中的 `USER`、`ASSISTANT`、`SYSTEM` 字符串。
- 记录表索引已调整为匹配当前记录列表查询的 `(yonghu_id, xiugai_shijian)`，并移除外键中不会使用的 `ON UPDATE CASCADE`。
- 记录修改已明确为标签和正文传入 `null` 时保留原值、空字符串时清空；用户资料和记录修改均改为定向更新字段。
- AI候选正文已增加2000字符硬校验，JSON解析会拒绝尾随内容；同一会话的请求串行执行，失败时删除本次新增的人类消息。
- AI连接测试改为仅在模型回复非空白时判定成功；回复空白或调用异常时统一判定失败。
- Vue前端已迁移到 `src/main/frontend`，使用Vue Router、Pinia、Axios和Element Plus实现登录、记录编辑、AI对话、个人资料和管理员用户管理。
- 前后端已改为同源部署，业务接口统一使用 `/api` 前缀；Maven会自动构建前端并打入Spring Boot Jar。
- 直接访问或刷新 `/login` 会回退到前端入口，认证拦截器只处理 `/api/**`，不会拦截前端静态资源。
- 标记完成前会先保存编辑器当前内容，避免数据库正文与界面正文不一致。
- 记录列表已支持关键词、题材、状态筛选和 MyBatis-Plus 分页，前端处理删除后页码回退和当前页选择状态清理。
- 会话已增加名称和创建时间，并按创建时间及ID倒序排列；前端显示会话名称。
- ChatMemory 已升级为保留最近12条消息，并将更早消息压缩为最多4000字符的历史摘要。
- 前端页面和 Element Plus 组件已按路由和组件异步加载；Element Plus 改为组件级入口导入，生产构建的组件 JavaScript 块已从约926KB降至约359KB。
- 当前已实际完成前端生产构建、单Jar打包和主要业务接口真实联调。

## 2. 已确定的AI对话设计

### 2.1 正文和请求

- 允许创建正文为空的草稿。
- 每次AI请求中的编辑器当前正文必须非空且不超过2000个字符，但可以尚未保存。
- 每次对话都把编辑器当前正文放入 `DuihuaDTO.zhengwen` 传给AI。
- 请求中的正文只作为AI上下文，不自动写入数据库。
- `command = 0` 的 `neirong` 必填并保存为人类消息；`command = 1`、`2` 不保存按钮操作。

### 2.2 状态和命令

- 状态枚举已由 `HuihuaZhuangtai` 改名为 `DuihuaZhuangtai`，表示本次AI处理结果，不是持久化的会话状态。
- `CONTINUE` 表示继续交流，`WAIT` 表示AI建议用户选择，`OVER` 表示本次生成完成并返回候选正文。
- `OVER` 不关闭会话，用户之后仍可继续补充新的想法。
- 不把 `CONTINUE/WAIT/OVER` 写入 `Huihua` 或 `Xiaoxi`，不建立强制状态机。
- 三个命令都可以随时调用，不要求先进入 `WAIT`。
- `duihua()` 内部按命令校验结果：命令0可以返回任意合法状态，命令1只接受 `CONTINUE`，命令2只接受 `OVER`。

### 2.3 AI判断

- 已删除 `huiheTongji()`、`huoquTishici()`、`duihuaZhuangtaiJiaoyan()` 和固定5至8轮规则。
- 已删除 `JIXU`、`ZHUIWEN`、`SHENGCHENG` 三个固定回合提示词。
- AI根据题目、题材、标签、编辑器当前正文、会话历史和用户意图决定状态。
- 用户明确要求生成、总结、扩写或润色时应返回 `OVER`。
- 用户明确要求继续讨论或暂不生成时不得返回 `OVER`。
- 用户没有明确倾向时，AI可以自主决定继续、等待或生成。
- 用户没有指定处理方式时，AI可以自行判断总结、扩写或润色；判断不清时可以返回 `WAIT`。
- AI不得编造用户没有提供的重要事实。

### 2.4 候选正文

- `OVER` 的 `quanwen` 只作为普通AI消息保存，不立即更新 `Jilu.zhengwen`。
- 前端根据本次 `DuihuaVO.zhuangtai` 临时标记候选正文，不增加消息类型或状态字段。
- 用户点击使用后，把候选正文放入编辑器；用户可以继续修改或直接确认。
- 用户确认时调用现有 `/api/jilu/modify` 保存正文，并将记录状态设为 `DRAFT`。
- 不新增后端候选确认接口、候选正文表或文章版本表。
- 后续AI请求继续携带编辑器当前正文，因此AI可以看到尚未保存的修改。
- 刷新后临时候选标记和未保存正文可以丢失；候选文字仍保留在普通消息历史中。

## 3. 后续改进顺序

1. 增加自动保存或更细的离开页面恢复机制，进一步减少未保存正文丢失。

## 4. 有意保留的设计

| 设计 | 保留原因或当前约定 |
| --- | --- |
| 不持久化AI对话状态 | 允许用户中断后带着新想法继续，不用严格状态机限制命令 |
| 不限制固定问答轮数 | 用户意图优先，允许提前生成或继续深入交流 |
| `OVER` 后仍可继续对话 | `OVER` 只表示本次生成完成，不表示会话永久关闭 |
| 候选正文只由前端临时标记 | 第一版保持数据库和后端流程简单 |
| 修改密码使用查询参数 `mima1`、`mima2` | 用户明确决定保留，不改成JSON请求体 |
| 已处理异常仍返回HTTP 200，通过 `Result.code` 表示结果 | 当前前后端联调约定 |
| 认证和管理员授权共用一个 `AuthInterceptor` | 项目规模小，保持简单 |
| 管理员修改接口使用 `GuanliyuanXiugaiDTO` | 空白密码保留原值，非空密码与其他字段执行参数校验 |
| 手机号、身份证号和邮箱允许重复 | 当前业务规则 |
| 不使用Spring Security、JWT或OAuth | 使用Redis Token完成简单认证 |

## 5. 已经修复的重要问题

- 管理员权限判断已经改为使用Spring MVC实际匹配路径。
- AI远程调用不再放入长数据库事务。
- AI失败时不会保存空AI回复或修改正文，并会补偿删除本次新增的人类消息。
- AI返回重复、额外、缺少或类型错误的协议字段时会被拒绝。
- AI对话请求已经统一为 `DuihuaDTO`，不再同时维护重复的消息请求DTO。
- `DuihuaController` 类名、方法名和 `/api/duihua` 资源路径已经统一。
- 状态枚举已由 `HuihuaZhuangtai` 重命名为 `DuihuaZhuangtai`，源码中全部引用已经同步。
- 固定回合数统计、按回合提示词和状态限制已经删除，普通对话改为按用户意图判断。
- `OVER` 已改为只保存普通AI消息；原 `baocunAIShengchengJieguo()`、更新记录的事务代码和无效依赖已经删除。
- `JiluXiugaiDTO.zhengwen` 和 `DuihuaDTO.zhengwen` 已增加2000字符上限，AI提示词同步限制候选正文长度。
- 会话删除接口已实现为 `DELETE /api/huihua/delete`，删除会话时由数据库级联删除其全部消息。
- 管理员修改接口已由直接接收 `Yonghu` 改为接收 `GuanliyuanXiugaiDTO`；空白密码不再重置，非空密码和其他字段已增加校验。
- 普通用户资料修改和记录修改均已改为只更新接口负责的字段，不再回写完整旧实体。
- `ChatServiceImpl.lianjie()` 已改为只有模型回复非空白时才判定连接成功，回复空白或调用异常时统一返回失败。
- 前端已迁移到 `src/main/frontend` 并打入单个Spring Boot Jar，不再单独部署静态站点。
- 前端API与路由的循环依赖已拆开，Token常量已移到独立文件。
- 标记完成会先保存编辑器当前内容，不再使用数据库中的旧正文完成记录。
