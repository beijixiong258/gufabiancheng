# 实体类与数据库设计

> 技术版本见 [开发总览](./1.开发总览.md)。

## 1. 实体关系

```text
Yonghu 1 —— N Jilu 1 —— N Huihua 1 —— N Xiaoxi
```

- 一个用户可以拥有多条记录。
- 一条记录可以拥有多个会话，每个会话只属于一条记录。
- 一个会话可以拥有多条消息。

初稿、第一次润色和后续修改可以分别创建不同会话，因此记录与会话是明确的一对多关系。

目标数据库结构建立对应外键，并使用级联删除。

## 2. 命名映射

项目在实体类中使用 `@TableName`、`@TableId` 和 `@TableField` 显式声明表名、主键字段和普通字段。`application.yaml` 同时启用了：

```yaml
map-underscore-to-camel-case: true
```

虽然驼峰字段可以自动映射到数据库下划线字段，但当前实体仍采用全量显式注解，映射如下：

| Java字段 | 数据库字段 |
| --- | --- |
| `yonghuId` | `yonghu_id` |
| `jiluId` | `jilu_id` |
| `huihuaId` | `huihua_id` |
| `chuangjianShijian` | `chuangjian_shijian` |
| `xiugaiShijian` | `xiugai_shijian` |
| `chanshengShijian` | `chansheng_shijian` |

## 3. `Yonghu` 用户实体

对应数据库表：`yonghu`。

| Java字段 | Java类型 | 数据库类型 | 说明 |
| --- | --- | --- | --- |
| `id` | `String` | varchar(64) | 当前为32位无连字符UUID |
| `zhanghao` | `String` | varchar(32) | 登录账号，唯一 |
| `mima` | `String` | varchar(255) | 加密后的密码 |
| `dianhua` | `String` | varchar(32) | 手机号，可以为空 |
| `shenfenzheng` | `String` | varchar(32) | 身份证号，可以为空 |
| `youxiang` | `String` | varchar(255) | 邮箱，可以为空 |
| `quanxian` | `Quanxian` | tinyint | 用户权限 |

权限枚举：

```java
USER(0),
ADMIN(1);
```

业务要求：

- `zhanghao` 必须唯一。
- 普通注册的新用户 `quanxian` 固定为 `USER`；管理员新建用户可以指定权限。
- `mima` 保存密码哈希，不保存明文密码。
- 普通用户接口不能返回密码字段。
- `mima` 使用 `@JsonProperty(access = JsonProperty.Access.WRITE_ONLY)`，即使实体被意外序列化也不输出密码。管理员修改请求使用 `GuanliyuanXiugaiDTO`。
- 普通用户修改资料时必须提交合法账号；手机号和邮箱只允许修改为合法值，不能清空，传入 `null` 时保留原值。更新时只写入账号、手机号和邮箱，不回写完整用户实体。
- 管理员修改时，空白密码保留原密码，非空密码必须为6至32位；手机号、身份证号和邮箱可以修改为空字符串，传入 `null` 时保留原值。

## 4. `Jilu` 记录实体

对应数据库表：`jilu`。

| Java字段 | Java类型 | 数据库类型 | 说明 |
| --- | --- | --- | --- |
| `id` | `String` | varchar(64) | 当前为32位无连字符UUID |
| `timu` | `String` | varchar(255) | 题目 |
| `ticai` | `Ticai` | tinyint | 题材 |
| `biaoqian` | `String` | varchar(100) | 标签，可以为空 |
| `zhengwen` | `String` | longtext | 正文，可以为空 |
| `jiluZhuangtai` | `JiluZhuangtai` | tinyint | 草稿或完成，对应数据库列 `zhuangtai` |
| `yonghuId` | `String` | varchar(64) | 所属用户ID |
| `chuangjianShijian` | `LocalDateTime` | datetime(3) | 创建时间 |
| `xiugaiShijian` | `LocalDateTime` | datetime(3) | 修改时间 |

题材枚举：

```java
QITA(0),
RIJI(1),
WENXUE(2),
XUESHU(3),
HUIYI(4);
```

`JiluZhuangtai` 状态枚举：

```java
DRAFT(0),
FINISH(1);
```

业务要求：

- 新记录默认属于当前登录用户。
- 新记录默认状态为 `DRAFT`。
- 新建记录时只接收题目、题材和可空标签，不接收正文。
- 题目业务长度不超过50位，标签业务长度不超过20位；数据库字段保留 `varchar(255)` 和 `varchar(100)` 的预留空间。
- 新记录正文固定为空；正文通过输入DTO限制为最多2000个字符。用户可以先保存空草稿，AI请求使用编辑器中非空且不超过2000个字符的当前正文，只有用户确认候选后才通过记录修改接口写入。
- 修改记录时，`biaoqian` 和 `zhengwen` 传入 `null` 表示保留数据库原值，传入空字符串表示清空；修改后状态恢复为 `DRAFT`。
- Controller不能接收前端传入的 `yonghuId` 作为记录归属。
- 查询、修改和删除前都要检查记录是否属于当前用户。
- 已完成记录再次由用户修改正文，或确认AI候选正文并保存后恢复为 `DRAFT`；AI仅生成候选时不修改记录状态。
- `chuangjianShijian` 和 `xiugaiShijian` 的插入、更新策略均为 `FieldStrategy.NEVER`，MyBatis-Plus只负责查询映射，时间值分别由数据库的默认时间和 `ON UPDATE CURRENT_TIMESTAMP(3)` 维护。

## 5. `Huihua` 会话实体

对应数据库表：`huihua`。

| Java字段 | Java类型 | 数据库类型 | 说明 |
| --- | --- | --- | --- |
| `id` | `String` | varchar(64) | 当前为32位无连字符UUID |
| `jiluId` | `String` | varchar(64) | 所属记录ID |

业务要求：

- 创建会话前检查记录是否存在。
- 普通用户只能为自己的记录创建会话。
- 创建会话本身不自动调用AI。
- 会话不持久化 `CONTINUE/WAIT/OVER`；这些值只描述本次AI处理结果。
- `OVER` 后仍可继续使用同一会话。
- 删除记录后会话由数据库级联删除。

## 6. `Xiaoxi` 消息实体

对应数据库表：`xiaoxi`。

| Java字段 | Java类型 | 数据库类型 | 说明 |
| --- | --- | --- | --- |
| `id` | `Long` | bigint auto_increment | MySQL自增消息ID，用于唯一标识和排序 |
| `huihuaId` | `String` | varchar(64) | 所属会话ID |
| `type` | `XiaoxiLeixing` | varchar(20) | `USER`、`ASSISTANT` 或 `SYSTEM` |
| `neirong` | `String` | longtext | 消息正文 |
| `chanshengShijian` | `LocalDateTime` | datetime(3) | 消息产生时间 |

业务要求：

- 用户发送的消息设置 `type=USER`。
- AI回复设置 `type=ASSISTANT`。
- 系统提示消息设置 `type=SYSTEM`。
- Java代码使用 `XiaoxiLeixing` 枚举表示消息类型，枚举通过 `@EnumValue` 显式映射为 `USER`、`ASSISTANT`、`SYSTEM` 字符串。
- AI候选正文也作为普通AI消息保存，不增加候选类型或状态字段。
- 前端只根据本次 `DuihuaVO.zhuangtai` 临时标记候选正文。
- 新消息ID由MySQL自增生成，不在Java代码中手动计算序号。
- 查询消息时按照 `id` 升序排列。

## 7. 主键设计

`Yonghu`、`Jilu` 和 `Huihua` 使用UUID字符串主键。当前通过 `Utils.generateId()` 去除UUID连字符，生成32位字符串，例如：

```text
550e8400e29b41d4a716446655440000
```

这三个实体的Java字段使用 `String`，数据库主键和对应外键使用 `VARCHAR(64)`。`Xiaoxi` 是例外：Java字段使用 `Long`，数据库使用 `BIGINT AUTO_INCREMENT`，由MySQL生成消息ID。

## 8. 当前对应情况

当前四个实体均已显式标注表名、主键字段和全部普通字段：

```text
Yonghu  → yonghu
Jilu    → jilu
Huihua  → huihua
Xiaoxi  → xiaoxi
```

`Yonghu`、`Jilu`、`Huihua` 的 `id` 使用 `@TableId("id")`，`Xiaoxi.id` 使用 `@TableId(value = "id", type = IdType.AUTO)`；其余属性均使用 `@TableField` 对应数据库字段。

以下SQL是新建数据库时使用的完整目标结构。

## 9. 完整数据库建表语句

适用数据库版本：MySQL Server 8.0系列；具体补丁版本以当前Docker镜像为准。

以下SQL适合手动从空MySQL实例执行，按照外键依赖顺序创建数据库、用户表、记录表、会话表和消息表。项目中的 `src/main/resources/schema.sql` 由应用在已经选定的 `jiluzhushou` 数据库中执行，因此只包含建表语句。两处SQL均不包含删除数据库或删除表的语句。

```sql
CREATE DATABASE IF NOT EXISTS jiluzhushou
    DEFAULT CHARACTER SET utf8mb4
    COLLATE utf8mb4_unicode_ci;

USE jiluzhushou;

CREATE TABLE IF NOT EXISTS yonghu
(
    id            VARCHAR(64)     NOT NULL COMMENT '用户ID',
    zhanghao      VARCHAR(32)  NOT NULL COMMENT '登录账号',
    mima          VARCHAR(255) NOT NULL COMMENT '密码哈希',
    dianhua       VARCHAR(32)  NULL COMMENT '手机号',
    shenfenzheng  VARCHAR(32)  NULL COMMENT '大陆身份证号',
    youxiang      VARCHAR(255) NULL COMMENT '邮箱',
    quanxian      TINYINT      NOT NULL DEFAULT 0 COMMENT '权限：0普通用户，1管理员',

    PRIMARY KEY (id),
    UNIQUE KEY uk_yonghu_zhanghao (zhanghao),
    CONSTRAINT ck_yonghu_quanxian
        CHECK (quanxian IN (0, 1))
)
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_unicode_ci
    COMMENT = '用户表';

CREATE TABLE IF NOT EXISTS jilu
(
    id                   VARCHAR(64)     NOT NULL COMMENT '记录ID',
    timu                 VARCHAR(255) NOT NULL COMMENT '题目',
    ticai                TINYINT      NOT NULL DEFAULT 0 COMMENT '题材：0其他，1日记，2文学，3学术，4会议',
    biaoqian             VARCHAR(100) NULL COMMENT '用于分组的标签，可以为空',
    zhengwen             LONGTEXT     NULL COMMENT '当前正文，可以为空',
    zhuangtai            TINYINT      NOT NULL DEFAULT 0 COMMENT '状态：0草稿，1完成',
    yonghu_id            VARCHAR(64)     NOT NULL COMMENT '所属用户ID',
    chuangjian_shijian   DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) COMMENT '创建时间',
    xiugai_shijian       DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3)
        ON UPDATE CURRENT_TIMESTAMP(3) COMMENT '修改时间',

    PRIMARY KEY (id),
    KEY idx_jilu_yonghu_xiugai_shijian (yonghu_id, xiugai_shijian),
    CONSTRAINT fk_jilu_yonghu
        FOREIGN KEY (yonghu_id) REFERENCES yonghu (id)
            ON DELETE CASCADE,
    CONSTRAINT ck_jilu_ticai
        CHECK (ticai IN (0, 1, 2, 3, 4)),
    CONSTRAINT ck_jilu_zhuangtai
        CHECK (zhuangtai IN (0, 1))
)
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_unicode_ci
    COMMENT = '记录表';

CREATE TABLE IF NOT EXISTS huihua
(
    id       VARCHAR(64) NOT NULL COMMENT '会话ID',
    jilu_id  VARCHAR(64) NOT NULL COMMENT '所属记录ID',

    PRIMARY KEY (id),
    KEY idx_huihua_jilu_id (jilu_id),
    CONSTRAINT fk_huihua_jilu
        FOREIGN KEY (jilu_id) REFERENCES jilu (id)
            ON DELETE CASCADE
)
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_unicode_ci
    COMMENT = '会话表';

CREATE TABLE IF NOT EXISTS xiaoxi
(
    id                    BIGINT       NOT NULL AUTO_INCREMENT COMMENT '消息ID',
    huihua_id             VARCHAR(64)  NOT NULL COMMENT '所属会话ID',
    type                  VARCHAR(20)  NOT NULL COMMENT '消息类型：USER、ASSISTANT、SYSTEM',
    neirong               LONGTEXT     NOT NULL COMMENT '消息内容',
    chansheng_shijian     DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) COMMENT '消息产生时间',

    PRIMARY KEY (id),
    KEY idx_xiaoxi_huihua_id_id (huihua_id, id),
    CONSTRAINT fk_xiaoxi_huihua
        FOREIGN KEY (huihua_id) REFERENCES huihua (id)
            ON DELETE CASCADE,
    CONSTRAINT ck_xiaoxi_type
        CHECK (type IN ('USER', 'ASSISTANT', 'SYSTEM'))
)
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_unicode_ci
    COMMENT = '会话消息表';
```
